{% extends 'base.html.twig' %}

{% block title %}Admin | Game create/edit{% endblock %}

{% block body %}
    <h2>Create/edit game type</h2>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.rules) }}
        {{ form_row(form.minRatingForMasterclass) }}

        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}
{% endblock %}
